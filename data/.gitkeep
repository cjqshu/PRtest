; .gitkeep file is a placeholder file used in Git repositories to allow tracking of otherwise empty directories. 
; .gitkeep文件是创建在一个空目录下的标识性文件，其作用是为了使Git保留该空目录。有了.gitkeep文件后就不是空目录了, 就能被Git管理, 从而保持项目结构的完整性。